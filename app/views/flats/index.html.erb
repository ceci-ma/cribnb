
  <div class="head border-top">
    <h1><%= "Where would you like to stay?" %></h1>
    <h3><%= "Pick a place to stay anywhere in the world with CriBnB. " %></h3>
  </div>

<!-- <div class="search">
  <form class="form-inline md-form form-sm active-cyan-2 mt-2">
    <input class="form-control form-control-sm mr-3 w-75" type="text" placeholder="Search"
      aria-label="Search">
    <i class="fas fa-search" aria-hidden="true"></i>
  </form>
</div> -->


<div class="flats_with_map d-flex border " >
  <div class="flats-container">
  <% @flats.each do |flat| %>
  <div class="flat-card d-flex ">
    <div id="carousel-<%= flat.id %>" class="carousel slide" >
    <div class="carousel-inner">
      <% flat.photos.each_with_index do |photo, index| %>
        <div class="carousel-item <%= "active" if index == 0 %>">
          <%= cl_image_tag(photo.key, width: 400, height: 300, crop: :fill, class: "m-3")%>
        </div>
      <% end %>
    </div>
    <a class="carousel-control-prev" href="#carousel-<%= flat.id %>" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carousel-<%= flat.id %>" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>

    <div class="flat-card-infos d-flex justify-content-between align-content-center">
      <div>
        <h2><%= link_to flat.title, flat_path(flat) %></h2>
        <p><%= flat.location %></p>
        <p class="small-print"><%= flat.guests %> guests • <%= flat.bedrooms %> bedrooms</p>
      </div>
      <div class="reviews align-self-center">
        <h4>£<%= flat.price %></h4>
      </div>
    </div>
  </div>
  <% end %>
  </div>

  <!-- <div class="map" style="min-width: 45%;"> -->
     <div
      id="map"
      style="width: 100%;
      height: 100vh;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  >

  <!-- </div> -->
</div>
</div>




