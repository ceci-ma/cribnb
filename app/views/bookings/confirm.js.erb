a = document.querySelector("#confirmed-status")
a.innerHTML = "Confirmed"

const initSweetalert = (selector, options = {}) => {
  const swalButton = document.querySelector(selector);
  if (swalButton) { // protect other pages
    swalButton.addEventListener('click', () => {
      swal(options);
    });
  }
};

initSweetalert('#sweet-alert-demo', {
  title: "Payment Confirmed",
  text: "Enjoy your Crib",
  icon: "success"
});


document.querySelector("#sweet-alert-demo").classList.add('hidden')
// document.querySelector(".booking-review-container").classList.remove('hidden')
